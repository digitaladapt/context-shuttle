# ── context-shuttle environment ──────────────────────────────────────
# Copy to .env.local (dev) or inject at runtime (prod). .env is never
# committed with secrets (Guiding Light §5.1); only safe defaults live here.

APP_ENV=dev
APP_SECRET=

# ── MCP identity ─────────────────────────────────────────────────────
# Name and version reported in MCP initialize responses and OpenAPI info.
MCP_SERVER_NAME=context-shuttle
MCP_SERVER_VERSION=1.0.0

# ── Logging ──────────────────────────────────────────────────────────
# Monolog writes JSON to stdout. Use LOG_LEVEL to raise/lower globally.
LOG_LEVEL=info