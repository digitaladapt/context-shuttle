# FrankenPHP app config for context-shuttle.
# Serves public/ with PHP; health endpoints bypass normal routing limits.

{
	# Listen on the standard FrankenPHP port inside the container
	admin off
}

{$SERVER_NAME:localhost} {
	root * public
	encode zstd gzip

	php_server
}